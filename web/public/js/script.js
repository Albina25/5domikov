/*
window.onscroll = function() {
    stickSection();
};

var bookedBlock = document.getElementById("bookedBlock");
var topPos = bookedBlock.offsetTop;

function stickSection() {
    if (window.pageYOffset >= (topPos - 80)) {
        bookedBlock.classList.add("sticky");
    } else {
        bookedBlock.classList.remove("sticky");
    }
}*/

/*let booking = document.querySelector('.choose-date-link');
booking.addEventListener('click', function () {
})*/
